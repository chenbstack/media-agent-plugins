# MoviePilot 数据导入插件

通过 MoviePilot 侧的 Media Agent 迁移桥分页读取数据，并调用 Media Agent 宿主提供的通用站点账号、订阅和整理记录能力完成增量导入。

- 插件自己维护连接、分页、增量 hash、暂存、字段转换和结果汇总。
- 宿主只提供权限化的领域能力，不包含 MoviePilot 专用逻辑。
- 下载历史和下载文件不会迁移；Downloads 通用 SDK 留给下载类插件使用。整理历史默认开启。
- 系统设置、插件配置、插件数据和订阅历史保存在插件私有数据库中作为快照，不写入宿主全局设置。
